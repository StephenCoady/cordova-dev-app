import { ApolloLink, Observable, Operation, NextLink } from 'apollo-link';
export default class QueueLink extends ApolloLink {
    private opQueue;
    private isOpen;
    open(): void;
    close(): void;
    request(operation: Operation, forward: NextLink): Observable<{}>;
    private cancelOperation(entry);
    private enqueue(entry);
}
